# Web Framework
fastapi==0.140.0
uvicorn[standard]==0.51.0

# Validation / settings
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# ORM (SQLAlchemy 2.0 async + asyncpg) and migrations
SQLAlchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
greenlet==3.5.4

# Auth / Security
python-jose[cryptography]==3.5.0
# pwdlib: aktiv gepflegter Ersatz für passlib (unmaintained seit 2020).
# Bestehende bcrypt-Hashes ($2b$...) bleiben verifizierbar.
pwdlib[bcrypt]==0.3.0
bcrypt==5.0.0

# File uploads (multipart) and helmet equivalents
python-multipart==0.0.32
secure==2.0.1
slowapi==0.1.10

# Excel / PDF / Swiss QR Bill
openpyxl==3.1.5
reportlab==5.0.0
qrbill==1.2.0
# svglib 2.x setzt neuere reportlab-Versionen voraus.
svglib==2.0.2
Pillow==12.3.0

# Email
fastapi-mail==1.6.5
aiosmtplib==5.1.2
certifi>=2024.7.4

# .env loading
python-dotenv==1.2.2

# Misc
itsdangerous==2.2.0
httpx==0.28.1

